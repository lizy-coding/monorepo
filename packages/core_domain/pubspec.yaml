name: core_domain
description: Domain layer models and utilities for the BLE instant messaging demo.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  json_annotation: ^4.9.0
  meta: ^1.11.0

dev_dependencies:
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
  test: ^1.25.2
