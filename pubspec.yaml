name: ble_chat_flutter_workspace
description: Workspace-level tooling and scripts for the Melos-managed ble_chat_flutter.
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  melos: ^3.1.1

